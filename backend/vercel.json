{
  "bunVersion": "1.x",
  "buildCommand": "bunx --bun prisma generate",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
