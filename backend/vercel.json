{
  "bunVersion": "1.x",
  "buildCommand": "bunx --bun prisma generate",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/cron/embeddings?limit=25&key=CHANGE_ME",
      "schedule": "*/15 * * * *"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
